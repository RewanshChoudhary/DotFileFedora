# /* ----  https://github.com/JaKooLit  ---- */  #
# Commands and Apps to be executed at launch

# HyprWave (music control widget)
exec-once = pgrep -x hyprwave >/dev/null || (command -v hyprwave >/dev/null && hyprwave || ([ -x "$HOME/.local/bin/hyprwave" ] && "$HOME/.local/bin/hyprwave"))
