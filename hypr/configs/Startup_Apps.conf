# /* ----  https://github.com/JaKooLit  ---- */  #
# Commands and Apps to be executed at launch (vendor defaults)
$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts
$lock = $scriptsDir/LockScreen.sh
$SwwwRandom = $UserScripts/WallpaperAutoChange.sh
$livewallpaper=""
$wallDIR = $HOME/Pictures/wallpapers  # change path manually here if needed

### wallpaper stuff ###
exec-once = swww-daemon --format xrgb
#exec-once = mpvpaper '*' -o "load-scripts=no no-audio --loop --hwdec=nvdec --vo=gpu --gpu-api=vulkan --video-sync=display-vdrop --interpolation=no --profile=fast --cache=no --demuxer-readahead-secs=1 --demuxer-max-bytes=16MiB --demuxer-max-back-bytes=4MiB --vd-lavc-threads=1 --framedrop=vo" $livewallpaper
# wallpaper random
#exec-once = $SwwwRandom $wallDIR # random wallpaper switcher every 30 minutes

### Startup ###
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = $HOME/.config/hypr/scripts/Dropterminal.sh kitty &
exec-once = $scriptsDir/Polkit.sh
exec-once = pgrep -x waybar >/dev/null || waybar
exec-once = $scriptsDir/StartQuickshell.sh ensure
exec-once = pgrep -x hypridle >/dev/null || hypridle
exec-once = $scriptsDir/Hyprsunset.sh init

# Clipboard manager
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Rainbow borders (disabled by default; use quick settings menu)
#exec-once = $UserScripts/RainbowBorders.sh


# Here are list of features available but disabled by default
# Persistent wallpaper
# exec-once = swww-daemon --format xrgb && swww img $wallDIR/mecha-nostalgia.png

# Gnome polkit for NixOS
#exec-once = $scriptsDir/Polkit-NixOS.sh

# xdg-desktop-portal-hyprland (should be auto starting. However, you can force to start)
#exec-once = $scriptsDir/PortalHyprland.sh
exec-once = $scriptsDir/KeybindsLayoutInit.sh
